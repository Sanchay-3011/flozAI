[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src

# Show more detailed output
addopts = -v --tb=short

# Coverage settings (optional)
# addopts = -v --tb=short --cov=flozai --cov-report=term-missing